name: time_block_recorder
description: 时间区块记录器
publish_to: 'none'
version: 2.3.0+5

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  path_provider: ^2.1.6
  path: ^1.9.1
  file_picker: ^12.1.2
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  cupertino_icons: ^1.0.9
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
